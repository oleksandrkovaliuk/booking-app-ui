export enum TypesOfSelections {
  UNSELECTED = "",
  WHERE = "where",
  DATE = "date",
  DATE_EXPERIENCES_CHECKIN = "date_experiences_chekin",
  DATE_EXPERIENCES_CHECKOUT = "date_experiences_checkout",
  GUEST = "guest",
}
